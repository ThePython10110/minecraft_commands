# Override commands if they already exist (such as /kill)
minecraft_commands_override (Override existing commands?) bool false
# Commands that only exist in one version will exist regardless of this setting.
minecraft_commands_edition (Minecraft edition to replicate) enum Java Java,Bedrock